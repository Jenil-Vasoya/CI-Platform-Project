

<partial name="_FirstNav">



    <style>
       

 .nav-pills .nav-link.active, .nav-pills .show > .nav-link {
  color: var(--bs-nav-pills-link-active-color);
  background-color: #ff7800;
}

 
.applyButton {
    border: 2px solid #F88634;
    border-radius: 100px;
    color: #F88634;
    background-color: white;
    padding-left: 20px;
    padding-right: 20px;
}

    .applyButton:hover {
        background-color: #F88634;
        color: white;
    }
    </style>



    <div class="container-fluid p-0">
        <div class="row flex-nowrap g-0">

            <div class="col-2 col-md-4 col-lg-3 col-xl-2 side-nav ">
                <div class="d-flex flex-column align-items-end align-items-sm-end min-vh-100">
                    <ul class="nav nav-pills flex-column align-items-center align-items-sm-center nav-list w-100" id="menu">


                        @foreach (var item in ViewBag.CMSList)
                        {
                            <li style="width: 100%;">
                                <a href="#" class="nav-link align-end btn applyButton border d-flex" id="nav-@item.Title-tab" data-bs-toggle="tab" data-bs-target="#nav-@item.Title" role="tab" aria-controls="nav-@item.Title" aria-selected="true">
                                    <span class="ms-1 text-start">@item.Title</span>
                                    
                                    <i class="bi bi-chevron-compact-right text-end"></i>
                                </a>
                            </li>
                        }
                    </ul>


                    <hr>

                </div>
            </div>


            <div class="col-10 col-md-8 col-lg-9 col-xl-10">

                


                <div class="m-2">
                    <div class="tab-content" id="nav-tabContent">

                        @foreach (var item in ViewBag.CMSList)
                        {
                            <div class="tab-pane fade show active" id="nav-@item.Title" role="tabpanel" aria-labelledby="nav-@item.Title-tab">
                                <div id="searchUser1" style="padding:10px;">
                                    <h4 class="pt-3 pb-2">@item.Description</h4>
                                </div>
                            </div>
                        }
                    </div>
                </div>

            </div>


        </div>
    </div>

